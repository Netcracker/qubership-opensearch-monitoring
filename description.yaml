build:
 networkRules: nc.product.dtrust
 env:
  type: microservice
  version: generic
publication:
 docker:
  - latest
  - timestamp
 files:
  INFRASTRUCTURE1.0:
   - target/*.zip
deploy:
 openshift:
  service: "elasticsearch-monitoring"
  templatePatching: disabled